/**
 * Active edge / lane hues — keep in sync with `tokens.css` (--flow-edge-*).
 * Used by TransactionFlowCanvas edgeStroke and phase label styling references.
 */
export const FLOW_PHASE_EDGE = {
  local: '#38bdf8',
  dispatch: '#a78bfa',
  async: '#34d399',
  failure: '#f87171',
  replay: '#fbbf24',
};
